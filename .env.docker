# Docker Compose environment variables
# These are only used by docker-compose to suppress warnings
# The actual application config comes from the mounted .env file

# Set dummy values to suppress docker-compose warnings
DYLD_LIBRARY_PATH=""
PKG_CONFIG_PATH=""
FIRECRAWL_API_KEY=""
OPENAI_API_KEY=""
